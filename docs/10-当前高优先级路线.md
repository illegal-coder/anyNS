# 当前高优先级路线

日期：2026-06-23

本文件覆盖原 P0-P4 路线中的近期执行顺序。原架构原则不变：PowerDNS 是主解析与管理路径，CoreDNS 仍是可选安全 profile。

## Stage 1 closeout：HNS TLD + DNSSEC + certificate demo

Stage 1 baseline was `6a6adefa9cd513400c6226789ba38f1947cec928`. The next development stage should use the documentation closeout commit as its baseline after this file is committed and pushed.

Sanitized stage evidence:

- Build gate passed for the Stage 1 changed surface: `go test -buildvcs=false ./...`, `go vet -buildvcs=false ./...`, local binary builds, frontend unit/ESLint/build, shell syntax, Compose model validation, `private-ca-certificates`, `docker-dnssec-validation`, and `hns-private-ca-demo`.
- GitHub Actions `CI` passed on Stage 1 commits including `999fd20cf7f2410841d958468b0866da5824375e`; the workflow covers Go test/vet/build, frontend unit/ESLint/build, shell syntax, Compose model validation, and `anyns-source-policy`.
- White-box review of private-root certificate download, CRL, OCSP, trust handoff, trust-store handoff, CRL publication path handling, and the HNS private-CA demo found no confirmed security vulnerability.
- Black-box testing against a disposable Docker stack confirmed single-label `example.` storage, `.hns` display alias separation, DNSSEC `ad` validation, private-root explicit HTTPS trust, wrong-host rejection, TLSA publication, OCSP `good`/`revoked`, public CRL reachability, method restrictions, malformed-path non-disclosure, and HNS child-zone rejection.
- Remediation fixed the low-severity cross-zone TLSA publish status mapping and hardened CRL child-path behavior. The demo profile remains intentionally unauthenticated for disposable acceptance use only and must not be reused as a production deployment profile.
- DNS/root security work added external DNSLog/OOB callback-platform rejection with explicit config switch, normalized platform-domain list, allowlist precedence, Runtime `403`/DNS `SERVFAIL` block behavior, and distinct DNSLog audit output.

Remaining server-only or deferred work:

- Production parent-zone DS publication, real external validating resolver behavior, synced live hnsd positive `NOERROR` evidence, production reverse-proxy/TLS deployment review, external RPC/API smoke, long/capacity tests, standard gc Go race gate, `govulncheck`, dedicated DNSLog-platform Selenium save/read-only coverage, broader root/HNS malformed wire/API matrix, and cross-host disaster recovery.

## P0：SOA、根域名与顶级域名解析

目标是把 HNS 名称按真正的单标签顶级权威区管理，而不是把 `.hns` 当作公开 DNS 子区。

交付范围：

- 单标签 HNS/IDNA Zone 创建、读取、删除和冲突检查。
- Zone apex 的 SOA、NS、A/AAAA glue 与 serial 生命周期。
- SOA 字段编辑、默认值、边界校验、原子更新和审计。
- HNS 链上 NS/GLUE 展示与 PowerDNS 权威记录的一致性检查。
- Authoritative、Recursor、hnsd、DoH、DoT 的顶级域名查询链路。
- Cloudflare 风格的 Zone 概览、DNS 记录、SOA/DNSSEC 管理体验。

验收：

- `example.` 和 Unicode 单标签 Zone 可返回正确 SOA/NS/A/AAAA。
- `.hns` 只作为用户可读别名出现，不进入 PowerDNS Zone 主键。
- `www.example` 不能被误创建为 HNS 顶级 Zone。
- SOA serial 更新、错误输入、越区记录和权限矩阵都有自动化测试。
- Selenium 覆盖桌面与移动端创建 Zone、编辑记录、查看 SOA/DNSSEC。

当前证据：

- [x] `bash tests/acceptance/docker-soa-tld.sh` 在隔离 gsqlite 拓扑中验证 ASCII/Unicode 单标签 Zone、`.hns` 主键剥离、非法子 Zone 拒绝、apex SOA/NS、A/AAAA glue、PowerDNS Authoritative AA 响应、Recursor 一致响应和 SOA serial 递增。
- [x] 结构化 SOA 编辑 API 与管理页面已支持 primary NS、hostmaster、TTL、refresh/retry/expire/minimum、显式 serial 校验和空 serial 自动递增；`bash tests/acceptance/selenium-admin.sh` 覆盖桌面端 Zone 创建后的 SOA 修改与记录表刷新。
- [x] `bash tests/acceptance/selenium-admin.sh` 覆盖移动端 HNS 单标签 Zone 创建、SOA Refresh 修改、记录表刷新和清理恢复。
- [x] `bash tests/acceptance/docker-soa-tld.sh` 覆盖 `example.` 单标签 TLD 经 BIND 转发后的明文 DNS、DoT 与 DoH SOA 响应，并验证错误 TLS 主机名拒绝。
- [x] `bash tests/acceptance/docker-hnsd-integration.sh` 默认 no-live 模式验证 hnsd、Recursor、BIND DoT/DoH profile model 与 anyNS 配置渲染；live hnsd P2P/SPV 运行仍需 `ANYNS_RUN_DOCKER_HNSD_INTEGRATION=1` 显式开启。
- [x] `ANYNS_RUN_DOCKER_HNSD_INTEGRATION=1 bash tests/acceptance/docker-hnsd-integration.sh` 验证 opt-in live hnsd 单标签链路会经过 anyNS Runtime `hns` 路由、PowerDNS Recursor、BIND 明文 DNS、DoT 与 DoH；新启动 hnsd 未同步时允许 `SERVFAIL`，但禁止回退到 static HNS fixture。
- [x] `bash tests/acceptance/hns-private-ca-demo.sh` 使用一次性 Admin/private-ca + PowerDNS/BIND 拓扑验证 `example.hns` 以 `example.` 单标签 TLD 存储和解析，并覆盖 apex SOA、NS、A/AAAA glue、DNSKEY、DS、Authoritative RRSIG 与 Recursor `ad` 验证。
- [x] HNS Zone 创建会在调用 PowerDNS 前拒绝 malformed 单标签输入，包括 service-label、斜杠、反斜杠、百分号、分号、引号、wildcard、首尾 hyphen 和多标签名称；`go test -buildvcs=false ./internal/powerdns ./internal/adminapi -run 'TestCreateHNSZoneRejectsMalformedTopLevelLabels|TestCreateHNSZoneRejectsMalformedLabelsBeforePowerDNS|TestCreateUnicodeHNSZone'` 覆盖直接 client 与 Admin API 边界。
- [x] RRSet mutation 在调用 PowerDNS 前拒绝 DNSKEY 非 apex owner、TLSA 非 `_port._protocol` owner、literal control-character presentation 和超过 4096 bytes 的单条记录内容；`go test -buildvcs=false ./internal/powerdns ./internal/adminapi -run 'TestPatchAuthoritativeZoneRejectsUnsafeRRSetMutations|TestPowerDNSRRSetPatchRejectsUnsafeMutationsBeforePowerDNS|TestPatchAuthoritativeZoneValidatesDNSSECDANEAndCAARecords|TestPowerDNSZoneDetailAndRRSetPatch'` 覆盖直接 client 与 Admin API 边界。
- [x] HNS DNS backend 在构建 UDP/TCP wire query 前校验 `.hns`/`.hsd` 映射后的 root label，拒绝斜杠、反斜杠、百分号、分号、引号、wildcard、首尾 hyphen 等 malformed root label，同时保留 `_443._tcp.example.hns` 这类服务标签子名；`go test -buildvcs=false ./internal/plugins/hns -run 'TestDNSBackendQNameTranslation|TestDNSBackendQNameRejectsMalformedRootLabels|TestDNSBackendResolveRejectsMalformedRootLabelBeforeExchange|TestDNSBackendFallsBackToTCPOnTruncatedUDP'` 覆盖 name translation、拒绝路径和未触发 DNS exchange。

## P0：证书与信任根管理

现有 ACME DNS-01 保留。新增能力采用 clean-room 实现，不复制 `eskimo/certy` 源码或其 PKI.js 副本。

信任模式必须明确分离：

1. `acme`：面向公开 WebPKI 可验证名称。
2. `private-ca`：由部署者管理私有根 CA，适用于 HNS、内网和受控客户端。
3. `browser-local`：可选的浏览器本地自签名叶证书与 TLSA 生成；私钥不离开浏览器。该模式不是根 CA。

交付范围：

- 私有根 CA 创建、导入、禁用、轮换、备份状态和证书指纹。
- 使用活动根 CA 签发叶证书，支持 SAN、受控 wildcard、有效期和用途约束。
- 证书清单、到期状态、下载、续期、吊销、TLSA 生成/发布和审计。
- 根私钥与叶私钥 `0600` 存储、API 不返回私钥、日志不记录密钥或 challenge。
- 明确显示“公开可信”“私有信任”“自签名”的差异。

验收：

- 根 CA 具备正确的 BasicConstraints、KeyUsage、SKI/AKI 和随机 serial。
- 叶证书不能继续签发 CA，SAN/有效期不得越过根证书策略。
- 私钥非披露、权限、备份恢复、轮换、吊销和并发签发均有测试。
- DANE/TLSA 仅在 DNSSEC 信任链或明确私有信任策略下使用。

当前证据：

- [x] `private-ca` issuer mode 已与 ACME 显式分离；clean-room 私有根签发器使用 Go `crypto/x509` 创建/加载本地根 CA，根私钥以 `0600` 存储，叶证书包含 SAN/serverAuth、SKI/AKI、随机 serial 和根链。
- [x] `bash tests/acceptance/private-ca-certificates.sh` 使用隔离 Compose profile 构建 Admin 镜像并验证 `private-ca` 模式签发、证书下载不含私钥、SAN/serverAuth/非 CA 约束、根链验证、根/叶私钥 `0600`、重启持久化和容器内备份恢复。
- [x] `GET /api/v1/certificates/private-ca/root` 返回私有根 CA subject、serial、有效期、SHA-256 指纹、SKI/AKI、KeyUsage 和根私钥权限状态，不返回 PEM 或私钥材料。
- [x] `GET /api/v1/certificates/private-ca/root/certificate` 显式下载当前活动私有根 CA 公开证书 PEM，要求 `certificates:read`，支持 `HEAD`，不返回根私钥、叶私钥或存储路径；`go test -buildvcs=false ./internal/adminapi -run 'TestPrivateCARootCertificateRequiresReadScopeAndReturnsOnlyRootPEM|TestPrivateCACertificateOrderUsesLocalIssuer|TestCapabilitiesDescribeAvailabilityAndWritableState'` 覆盖鉴权、单证书 PEM 和非披露。
- [x] `GET /api/v1/certificates/private-ca/root/trust` 返回私有根信任交接元数据、根证书/CRL URL、安装目标和操作员动作，明确 `public_webpki:false` 与显式信任要求；`go test -buildvcs=false ./internal/adminapi -run 'TestPrivateCARootTrustHandoffRequiresReadScopeAndReturnsNoSecrets|TestPrivateCARootCertificateRequiresReadScopeAndReturnsOnlyRootPEM|TestCapabilitiesDescribeAvailabilityAndWritableState'` 覆盖 scope、capability 暴露、公开 CRL URL 和非披露。
- [x] `GET /api/v1/certificates/private-ca/root/trust-store/handoff` 返回生产 trust store 操作员交接元数据，覆盖 Linux、macOS、Windows、浏览器、MDM 和容器镜像目标，明确 `mutates_client_trust:false`、不自动安装信任、不提升为公开 WebPKI；`go test -buildvcs=false ./internal/adminapi -run 'TestPrivateCATrustStoreHandoffRequiresReadScopeAndDoesNotMutateTrust|TestPrivateCARootTrustHandoffRequiresReadScopeAndReturnsNoSecrets|TestCapabilitiesDescribeAvailabilityAndWritableState'` 覆盖 scope、capability、目标清单、guardrail 和非披露。
- [x] `PATCH /api/v1/certificates/private-ca/root` 支持禁用/启用私有根 CA；禁用状态持久化并阻止新的 private-ca 叶证书签发，管理审计不记录 PEM 或私钥。
- [x] `bash tests/acceptance/private-ca-certificates.sh` 覆盖 private CA 证书清单状态、有效期窗口、TLSA 生成不发布、强制续期与 `renewal_of` 链接、原证书吊销、吊销后禁止续期、吊销证书下载私钥非披露，以及重启和备份恢复后的状态持久化。
- [x] `go test -buildvcs=false ./internal/certificates` 覆盖并发提交多个签发任务时全部进入 issued 清单、有效期窗口存在、公开清单不泄露 idempotency key，并验证底层 issuer 最大并发为 1。
- [x] `POST /api/v1/certificates/private-ca/root/backup-status` 记录当前根 CA 指纹的备份证据；根元数据返回 `missing`、`current` 或 `stale`，审计和响应不包含 PEM、路径或私钥。
- [x] Certificates 管理页面显示 ACME/private-root 信任模式、私有根指纹、备份状态、根密钥状态和证书清单计数；前端单元测试、ESLint、生产构建和 Selenium 桌面/移动端渲染验证不显示私钥或存储路径。
- [x] `POST /api/v1/certificates/private-ca/root/import` 支持导入自签名私有根 CA，验证 CA 约束、SKI/AKI、公私钥匹配和未过期状态；导入后重新加载 issuer，审计和响应不包含 PEM 或私钥。
- [x] `POST /api/v1/certificates/private-ca/root/rotate` 生成新的本地活动根并重新加载 issuer；旧备份标记变为 `stale`，后续叶证书由轮换根签发，审计和响应不包含 PEM、路径或私钥。
- [x] `GET /api/v1/certificates/private-ca/crl` 返回当前活动根签名的 PEM CRL，覆盖已标记 revoked 且由当前根签发的叶证书 serial，不返回证书链 PEM、路径或私钥。
- [x] `GET /api/v1/certificates/orders/{id}/ocsp` 为 private-ca 证书作业返回当前活动根签名的 DER OCSP 响应，issued 作业为 `good`、revoked 作业为 `revoked`，要求 `certificates:read`，支持 `HEAD`，不返回 PEM、私钥或存储路径；`go test -buildvcs=false ./internal/certificates ./internal/adminapi -run 'TestPrivateRootOCSPResponseReportsGoodAndRevokedLeaf|TestPrivateCAOCSPRequiresReadScopeAndReportsRevocation'` 覆盖签名解析、吊销状态、scope 和非披露。
- [x] `certificates.crl_distribution_url` 支持为新 private-ca 叶证书写入 CRL Distribution Point，配置校验要求 HTTP(S) URL；该能力会在 Admin 服务上发布配置 URL 的非保留路径，但不分发客户端 trust store。
- [x] Admin 服务会在 `certificates.crl_distribution_url` 的非保留 URL 路径提供无需管理凭据的 PEM CRL；`go test -buildvcs=false ./internal/adminapi -run 'TestPrivateCACRLPublicationUsesConfiguredPublicPath|TestCRLPublicationPathRejectsAPIAndReservedPaths|TestPrivateCACertificateOrderUsesLocalIssuer'` 验证认证 API 仍需 scope、公开路径返回 revoked serial、HEAD 无 body 且不泄露证书链或私钥。
- [x] `bash tests/acceptance/hns-private-ca-demo.sh` 在同一隔离工作流中验证 private-ca 签发 `example.`/`www.example.` 叶证书、根证书显式下载、证书链私钥非披露、使用 disposable origin 完成 HTTPS 握手、默认 trust store 拒绝私有根、错误主机名拒绝、TLSA `publish:true` 写入 `_443._tcp.www.example.`、Recursor `ad` DNSSEC 验证、OCSP `good`/`revoked`、作业吊销和公开 CRL 路径可达。
- [ ] 补齐跨主机灾备演练。

## P1：Cloudflare 风格控制面

借鉴功能分区和工作流，不复制品牌、文案或视觉资产：

- Zone 首页与状态。
- DNS Records、SOA、NS/Glue、DNSSEC。
- SSL/TLS、ACME、Private CA、证书清单。
- 插件、缓存、安全、审计和配置。
- capability-aware 菜单、细粒度 scope、只读降级和移动端。

前端继续使用现有 React/Vite 设计系统。复杂页面拆成独立组件和并行数据请求，避免扩大单体 `App.jsx` 和串行请求瀑布。

## P1：GitHub Actions

GitHub Actions 负责无密钥、可重复的快速门禁：

- Go test、vet 与 Linux 二进制编译。
- 前端 unit、ESLint 与生产构建。
- shell 语法和 Compose model 校验。
- 上传二进制与前端构建产物。

当前证据：

- [x] `CI` workflow 在 `main` 与 Pull Request 上并行执行 Go、前端和配置门禁；依赖缓存由 `go.sum` 与 `package-lock.json` 驱动。
- [x] 第三方 Action 固定到不可变 commit SHA，构建产物保留 7 天，Compose model 包含 SOA/TLD、private CA 与 DNSSEC valid/bogus 隔离拓扑。
- [x] `anyns-source-policy` 已纳入 `CI` 配置门禁，生成 Markdown step summary/artifact，并用 fixture 测试覆盖 Action 固定、workflow 写权限、私钥 PEM、私有自动化路径、pipe-to-shell、Compose privileged/host runtime、Go 弱 hash、`InsecureSkipVerify` 和 shell command construction 的 true/false positive 边界。
- [x] Stage 1 `main` CI run for `999fd20cf7f2410841d958468b0866da5824375e` succeeded with Go、前端、配置、shell、Compose model 和 source-policy gates；后续文档 PR `74e5685935205161cb112fac3e2e7dab0a40d5d1` 也通过 `CI`。

以下检查继续保留在服务器隔离环境：

- live hnsd/P2P 运行已具备 opt-in smoke gate；正向链上解析仍依赖 hnsd 同步状态，不纳入默认 deterministic 证据。
- Pebble DNS-01 全链路。
- Selenium 容器联调。
- 需要外部凭据的 RPC/API smoke。
- gpgsql 升级回滚、Docker 内灾备恢复和短程 load/soak 已有隔离门禁；长稳、外部后端和跨主机灾备演练仍需单独执行。

## P1：root/HNS 与 DNSLog 攻击抗性

交付范围：

- 单标签 root/HNS zone、apex SOA/NS/glue、DS、DNSKEY 和记录变更/query 路径的异常输入覆盖。
- Authoritative、Recursor、DoH、DoT 的有界畸形查询验证。
- 外部 DNSLog/OOB 回调平台拒绝能力，明确区别于 anyNS 内部 DNSLog 审计。
- 安全设置页面提供“拒绝 DNSLog 平台查询”开关和规范化平台域名列表编辑。

当前证据：

- [x] `security.reject_dnslog_platforms` 与 `security.dnslog_platform_domains` 已加入安全策略；列表按小写 ASCII/IDNA 规范化保存，拒绝空标签、控制字符、前导点、wildcard、正则和非法主机名。
- [x] Runtime 预查询规则按 `allowlist -> external-dnslog-platform -> denylist/sinkhole/analyzers` 执行；命中 exact 平台域或其子域时返回 HTTP `403` + DNS `SERVFAIL`，DNSLog 记录 `matched_rule=external-dnslog-platform`、`action=block`、`source_plugin=security`。
- [x] 前端 DNS 安全页已显示“拒绝 DNSLog 平台查询”开关和平台域名列表编辑器，说明该功能阻断外部回调平台且不禁用本地 DNSLog 审计。
- [x] `GOCACHE=/tmp/anyns-go-build go test -buildvcs=false ./internal/security ./internal/config ./internal/adminapi ./cmd/anyns-plugin-runtime` 覆盖 exact/subdomain、大小写、尾点、IDNA、标签边界绕过、allowlist 优先级、关闭模式、列表投毒、配置持久化和 Runtime DNSLog 审计形状。
- [x] `npm test` 覆盖前端平台域名编辑器规范化与 wildcard-like 行过滤。
- [x] `COMPOSE_PARALLEL_LIMIT=1 bash tests/acceptance/selenium-admin.sh` 在 DNSLog 防护 UI 变更后验证既有桌面 capability-aware、移动端 HNS SOA 和 Unicode HNS Zone 工作流仍通过。
- [x] `go test -buildvcs=false ./internal/powerdns ./internal/adminapi -run 'TestCreateHNSZoneRejectsMalformedTopLevelLabels|TestCreateHNSZoneRejectsMalformedLabelsBeforePowerDNS'` 覆盖 HNS 单标签创建 API 的 malformed label 拒绝，并验证失败请求不会转发到 PowerDNS。
- [x] `go test -buildvcs=false ./internal/powerdns ./internal/adminapi -run 'TestPatchAuthoritativeZoneRejectsUnsafeRRSetMutations|TestPowerDNSRRSetPatchRejectsUnsafeMutationsBeforePowerDNS'` 覆盖 RRSet mutation 的 DNSKEY/TLSA owner-boundary、literal control character 和 oversized content 拒绝，并验证失败请求不会转发到 PowerDNS。
- [x] `go test -buildvcs=false ./internal/plugins/hns -run 'TestDNSBackendQNameTranslation|TestDNSBackendQNameRejectsMalformedRootLabels|TestDNSBackendResolveRejectsMalformedRootLabelBeforeExchange|TestDNSBackendFallsBackToTCPOnTruncatedUDP'` 覆盖 HNS DNS backend wire query 构建前的 malformed root-label 拒绝，并验证失败请求不会触发 DNS exchange。
- [x] HNS DNS backend 的 wire query 编码和响应解析会拒绝超过 DNS 255-octet wire-name 上限的名称，并覆盖 compression pointer loop、out-of-range pointer、short pointer 和 unsupported label encoding；`go test -buildvcs=false ./internal/plugins/hns -run 'TestBuildDNSQueryRejectsOverlongQName|TestParseDNSResponseRejectsMalformedCompressedNames|TestParseDNSResponseRejectsOverlongExpandedName'` 验证该有界畸形包路径。
- [x] HNS DNS backend 响应解析会拒绝未设置 QR response bit 的伪响应和 echo question name 不匹配的响应，并过滤 answer section 中与查询名无关的 RR，避免恶意或异常 backend 响应把越权名称写入解析结果；`go test -buildvcs=false ./internal/plugins/hns -run 'TestParseDNSResponseRejectsNonResponseAndQuestionMismatch|TestParseDNSResponseFiltersUnrelatedAnswerNames'` 覆盖该边界。
- [x] HNS DNS backend 响应解析现在要求 backend echo 精确 1 个 question，且 qtype 与 IN class 均匹配查询；拒绝 zero/multiple question count、question type mismatch 和非 IN question class；`go test -buildvcs=false ./internal/plugins/hns -run 'TestParseDNSResponseRejectsQuestionCountAndClassMismatch'` 覆盖该 header/question 边界。
- [x] HNS DNS backend 对 NS/CNAME、MX 和 SRV 这类 name RDATA 强制校验解码后的 uncompressed/pointer 入口没有越过 RDLEN，防止 malformed RDATA 借后续 RR 字节被错误接受；`go test -buildvcs=false ./internal/plugins/hns -run 'TestParseDNSResponseSkipsNameRDATAThatExceedsRDLength'` 覆盖该 answer/RDATA 边界。
- [x] HNS DNS backend 会按 header count 跳过并边界校验 authority/additional RR section，拒绝 short trailing RR header/RDATA，同时继续忽略有效 additional 记录不污染结果；`go test -buildvcs=false ./internal/plugins/hns -run 'TestParseDNSResponseValidatesAuthorityAndAdditionalSections'` 覆盖该 section-count 边界。
- [ ] 尚需补齐 DNSLog 平台域名字段的专用 Selenium 保存/只读验证，以及 root/HNS wire/API 的更完整异常输入矩阵。

当前证据：

- [x] `bash tests/acceptance/docker-gpgsql-upgrade-rollback.sh` 使用临时 PostgreSQL 数据目录和固定镜像摘要，记录 Compose image set，验证 gpgsql baseline DNS/API 健康、预升级 `pg_dump`、失败升级数据变更、SQL 恢复、权威答案恢复和 PowerDNS API 恢复。
- [x] `bash tests/acceptance/docker-disaster-recovery.sh` 使用独立 source/target Compose projects 验证 PowerDNS gpgsql SQL 备份恢复、Admin/private-ca 证书存储 Docker-volume 备份恢复、目标 DNS/API 健康、证书清单恢复、证书下载私钥非披露和恢复后叶证书指纹一致。
- [x] `bash tests/acceptance/docker-load-soak.sh` 使用隔离 gpgsql + Admin/private-ca profile 运行短程 DNS/API 循环和 private-ca 证书批量签发，记录前后 `docker stats`、内存、磁盘和服务健康快照。
- [x] `bash tests/acceptance/docker-dnssec-validation.sh` 使用隔离 gsqlite Authoritative + Recursor profile 创建 `secure.` 与 `bogus.` 单标签 Zone，运行时注入有效 DS 与故意损坏 DS trust anchor，验证 secure 链返回 `NOERROR`、`ad` flag 和 RRSIG，bogus 链返回 `SERVFAIL` 且不带 `ad`。

## Agent 分工

| Agent | 当前主任务 |
| --- | --- |
| code | SOA/TLD 正确性；private CA/叶证书；DNS/SSL 控制面 |
| test | SOA/TLD wire/API/Selenium；证书安全与权限矩阵 |
| build | GitHub Actions；隔离 Compose；备份/恢复/升级 |
| docs | 信任边界、运维、状态与验收证据 |
